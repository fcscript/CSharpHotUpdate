1、调试端口目前默认是2600, 启动会开启这个端口，用于脚本调试， 请不要用杀毒软件禁止这个端口，不然无法调试

2、将你要编译的脚本目录或文件托到窗口中就可以了
   要先创建一个项目，然后才能托文件到项目工程

3、F7 编译工程

4、F5 启动编译好的字节码, 主函数是main(), 必须要有这个，不然无法启动。


工具还在开发中，望谅解
这个工具是用opgles 2.0 渲染的UI，字体是系统的宋体，如果出现界面文字显示不全的，麻烦与我联系